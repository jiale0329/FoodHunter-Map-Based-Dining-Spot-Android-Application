<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testmap">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestMap">
        <activity android:name=".SettleBill"
            android:label="Settle Bill"/>
        <activity
            android:name=".ViewBillRecordDetails"
            android:label="" />
        <activity
            android:name=".RecommendDiningSpot"
            android:label="Recommended Restaurants" />
        <activity
            android:name=".ViewRatedRestaurant"
            android:label="Restaurants You Rated" />
        <activity
            android:name=".ViewRestaurantProfile"
            android:label="" />
        <activity
            android:name=".DebtRecordByIndividual"
            android:label="Debt Record" />
        <activity
            android:name=".AddNewBillForm"
            android:label="New Bill Record" />
        <activity android:name=".UserLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserRegister" />
        <activity
            android:name=".SimilarityCalculation"
            android:label="Similarity Calculation" />
        <activity
            android:name=".ChoosePicture"
            android:label="Choose Picture" />
        <activity
            android:name=".PreviewSpinningWheelChoice"
            android:label="Spinning Wheel" />
        <activity
            android:name=".SpinningWheel"
            android:label="Spinning Wheel" />
        <activity
            android:name=".BillActivity"
            android:label="Bill Record" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
    </application>

</manifest>